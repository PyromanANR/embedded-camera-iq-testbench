# Python QA Test Station

FastAPI dashboard for image quality QA.

Run from the repository root:

```bat
scripts\windows\run_python_dashboard.bat
```

Open:

```text
http://127.0.0.1:8000
```

The dashboard connects to the C++ Camera Gateway at `http://127.0.0.1:8081` by default.

